----

## **测试机构：半舵底盘**

`调底盘`

1. bug：~~当vx = 0时，后6020解算角为8192，出错，当vx稍偏离0时，恢复正常~~ 原因：当后退时，偏转角为4096，目标角为中值+4096，此时超限，而PID_position_cal内对超限的处理是直接锁死，故卡在8192，改成-=8192后修复。
   
   ```c
   int16_t PID_Output_Cal_Position(PID_Config_t* pid, float Target, float Now, float limit)
   {   
       //合法
       if(motor_is_dial_n_limit == 0)
       {
           if(Target > 8192)
           {
               Target -= 8192;
               printf("Warning: Out of Range!\n");
           }
           else if(Target < -8192)//不会吧
           {
               Target = -8192;
               printf("Warning: Out of Range!\n");
   
          }
       }
   ......
   ```

2. 优化：半圈+反目标速度 / 过半圈？（其实还是有区别，）

3. 优化：转后再动？

`焊线和接线`

1. 滑环焊线，先上滑环。电源线5合1，先热缩，后粗热缩，最后才接头焊接打胶，剪线长度要与分电板沟通好。

2. 滑环CAN线，先细热缩后中热缩，CAN头少拔会松。

3. 分电板和电池，考虑其他线材长度，可选用单电池双分电板，其中板二用板一供电和通讯，接线同电机供电和通讯。

`与机械沟通：`

1. 在机械设计某些板件（如涉及到电机结构，哪一部分旋转）的时候，需要详细讨论可行性（比如6020中孔塞滑环，但孔壁是随着舵轮方向旋转的，此时滑环的摆放）

2. 电机摆放朝向（电源和CAN线）

3. 线材摩擦，电调距离调整（机械可能不知道某器材具体大小以及线长）（线长可能影响是否与附近板材或者电机摩擦）

4. 告诉机械底盘布置：电池（可能不止一个），电池架，分电板（不止一个），裁判系统，超级电容，主控板。

----

pow直接用乘法

两种供弹两块板

红外校准机械中值

重力校准机械中值

就近归位2048

当err绝对值>2048（四分之一圈）err负+4096，err正-4096；同时速度取反

    问题：如果处理的是err，那就必须塞在tar_ang之前，那速度取反，就必须让速度计算挪到角度之前 or 取反标志位（与换头有无冲突？）

    

8192 360°

4096 180°

2048 90°

1024 45°

<mark>**设备** --- **模块** --- **控制** --- **车型主控** --- **任务中心**</mark>
